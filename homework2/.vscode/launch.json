{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Django",
            "type": "node",
            "request": "launch",
            "preLaunchTask": "Run Django",
            "program": "${workspaceFolder}/run.sh"
        }
    ]
}
