This is a series of programs that I wrote that run basic pytest asserts to demonstrate that they work and that their final output is as expected.

Each assert is designed to demonstrate that the code works and the system can run, this is done by executing the command,

pytest -v

For some reason I cannot get Docker to run, I may uninstall and reinstall the client, but this would not launched through DevEdu so that may cause issues for future pushes.
